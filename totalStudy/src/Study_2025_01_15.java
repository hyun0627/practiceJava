 
public class Study_2025_01_15 {  
	public static void main(String[] args) { 
	
		 /* 1월 15일 필기 
		 * 1. switch-case문
		 * - 기본 문법:
		 *   switch(조건) {
		 *   case x :
		 *      실행문;
		 *      break;
		 *   case y :
		 *      실행문; ...
		 *      break; 
		 *   default(if else문에서 else의 의미) :
		 *      실행문;
		 *      break;
		 *   -> 조건에 해당하는 case문을 실행
		 *   break << 다중 실행을 막기 위해 사용(switch문을 탈출하는 용도)
		 *   }
		 *   
		 * 1-1. switch문으로 if문을 구현 못하는 경우
		 * - 1. 각 if문에서 비교되는 변수가 다를 경우
		 * - 2. 논리연산자가 사용되는 경우
		 *   -> 1.2번 Ex) if(a == 1) {} else if (b == 1) else {} // 비교하는 변수가 다른 경우에는 불가
		 * 1-2. switch문을 사용할 수 있는 경우
		 * - 1. ==로 하나의 변수만 비교할때
		 * ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
		 * 2. while문
		 * 2-1. while문 기본 문법
		 *       -> while(조건문) { 조건문이 참일때 실행문 }
		 * 2-1. do-while문
		 * 
		 */
	}
}
